# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj
version: 2

before:
  hooks:
    - go mod tidy
    - go generate ./...
    - go install github.com/tc-hib/go-winres@latest
    - go-winres make --in cmd/thor-launcher/winres/winres.json --out cmd/thor-launcher/rsrc --product-version={{ .Version }} --file-version={{ .Version }}

builds:
  - id: thor
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
      - -X github.com/ashir6892/thor/cmd/thor/internal.version={{ .Version }}
      - -X github.com/ashir6892/thor/cmd/thor/internal.gitCommit={{ .ShortCommit }}
      - -X github.com/ashir6892/thor/cmd/thor/internal.buildTime={{ .Date }}
      - -X github.com/ashir6892/thor/cmd/thor/internal.goVersion={{ .Env.GOVERSION }}
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/thor
    ignore:
      - goos: windows
        goarch: arm

  - id: thor-launcher
    binary: thor-launcher
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/thor-launcher
    ignore:
      - goos: windows
        goarch: arm

  - id: thor-launcher-tui
    binary: thor-launcher-tui
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/thor-launcher-tui
    ignore:
      - goos: windows
        goarch: arm

dockers_v2:
  - id: thor
    dockerfile: docker/Dockerfile.goreleaser
    extra_files:
      - docker/entrypoint.sh
    ids:
      - thor
    images:
      - "ghcr.io/{{ .Env.GITHUB_REPOSITORY_OWNER }}/thor"
      - "docker.io/{{ .Env.DOCKERHUB_IMAGE_NAME }}"
    tags:
      - "{{ .Tag }}"
      - "latest"
    platforms:
      - linux/amd64
      - linux/arm64
      - linux/riscv64

archives:
  - formats: [tar.gz]
    # this name template makes the OS and Arch compatible with the results of `uname`.
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
    format_overrides:
      - goos: windows
        formats: [zip]

nfpms:
  - id: thor
    builds:
      - thor
      - thor-launcher
      - thor-launcher-tui
    package_name: thor
    file_name_template: >-
      {{ .PackageName }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "arm64" }}aarch64
      {{- else if eq .Arch "arm" }}armv{{ .Arm }}
      {{- else }}{{ .Arch }}{{ end }}
    vendor: thor
    homepage: https://github.com/{{ .Env.GITHUB_REPOSITORY_OWNER }}/thor
    maintainer: thor contributors
    description: thor - a tool for managing and running tasks
    license: MIT
    formats:
      - rpm
      - deb
    bindir: /usr/bin

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

# upx:
#    - enabled: true
#      compress: best
#      lzma: true

release:
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
